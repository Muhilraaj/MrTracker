{
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "$BACKEND_URL/api/*"
    },
    {
      "route": "/apt1/*",
      "rewrite": "$BACKEND_URL/apt1/*"
    },
    {
      "route": "/auth/*",
      "rewrite": "$BACKEND_URL/auth/*"
    }
  ],
   "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "/auth/*"]
  }
}
