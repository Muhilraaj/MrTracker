{
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "$BACKEND_URL/api/*"
    },
     {
      "route": "/auth/*",
      "rewrite": "$BACKEND_URL/auth/*"
    }
  ],
   "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "/auth/*"]
  }
}
